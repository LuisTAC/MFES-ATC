class Airport

types
	public Direction  = <N>|<NE>|<E>|<SE>|<S>|<SW>|<W>|<NW>;
values
	
instance variables
	location : Coords;
	direction : Direction;
	planes : seq of Plane;
	
operations

	public Airport: int * int * Direction ==> Airport
	Airport(a,b, dir) ==
	(
		location := new Coords(a,b);
		direction := dir; 
		return self;
	);
	--TODO post
	
	public getLocation: () ==> Coords
	getLocation() == return location
	post RESULT = location;

functions
	
traces
	
end Airport